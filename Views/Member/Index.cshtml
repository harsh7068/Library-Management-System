@model IEnumerable<Library_Management_System.Models.tblMember>

@{
    ViewBag.Title = "Index";
    int role = Convert.ToInt32(Session["Role"]);
    if (role == 1)
    {
        Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    }
    else
    {
        Layout = "~/Views/Shared/_Layout.cshtml";
    }
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.memName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.memAddress)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.memPhoen)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.memName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.memAddress)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.memPhoen)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.memID }) |
            @Html.ActionLink("Details", "Details", new { id=item.memID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.memID })
        </td>
    </tr>
}

</table>
