@model IEnumerable<Library_Management_System.Models.tblAuthor>

@{
    ViewBag.Title = "Index";
    int role = Convert.ToInt32(Session["Role"]);
    if (role == 1)
    {
        Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    }
    else
    {
        Layout = "~/Views/Shared/_Layout.cshtml";
    }
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.authName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.authAddress)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.authPhone)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.authName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.authAddress)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.authPhone)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.authID }) |
            @Html.ActionLink("Details", "Details", new { id=item.authID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.authID })
        </td>
    </tr>
}

</table>
